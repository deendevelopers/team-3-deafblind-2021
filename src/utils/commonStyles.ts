import { StyleSheet } from 'react-native';

export const commonStyles = StyleSheet.create({
  containerStyle: {
    paddingHorizontal: 20,
    paddingVertical: 10,
  },
  flexRowCentre: {
    flexDirection: 'row',
    alignItems: 'center',
  },
});
