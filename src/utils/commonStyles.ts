import { StyleSheet } from 'react-native';

export const commonStyles = StyleSheet.create({
  containerStyle: {
    paddingHorizontal: 30,
    paddingVertical: 10,
  },
  flexRowCentre: {
    flexDirection: 'row',
    alignItems: 'center',
  },
});
